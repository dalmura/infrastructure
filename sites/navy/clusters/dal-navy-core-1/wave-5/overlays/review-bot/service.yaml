apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: review-bot
    app.kubernetes.io/name: review-bot
  name: review-bot
  namespace: review-bot
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: review-bot
    app.kubernetes.io/name: review-bot
