global:
  domain: argocd.navy.dalmura.cloud
  networkPolicy:
    create: true
  logging:
    format: json
    level: warn
  revisionHistoryLimit: 0

configs:
  rbac:
    policy.csv: |
      g, Admin, role:admin
      g, Viewer, role:readonly

controller:
  revisionHistoryLimit: 0
