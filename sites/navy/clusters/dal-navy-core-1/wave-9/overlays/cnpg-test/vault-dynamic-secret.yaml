apiVersion: generators.external-secrets.io/v1alpha1
kind: VaultDynamicSecret
metadata:
  name: "cnpg-test-db-backup"
spec:
  path: "/aws/creds/cnpg-test-db-backup"
  method: "GET"
  resultType: "Data"

  provider:
    server: "http://vault.vault.svc.cluster.local:8200"
    auth:
      kubernetes:
        mountPath: "kubernetes"
        role: "workload-reader-cnpg-test-db-backup"

        serviceAccountRef:
          name: "cnpg-test-sa"
