apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: "cnpg-test-db-backup"
spec:
  refreshInterval: "672h0m0s"  # aka 28d
  target:
    name: cnpg-test-db-backup
  dataFrom:
  - sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: VaultDynamicSecret
        name: "cnpg-test-db-backup"
