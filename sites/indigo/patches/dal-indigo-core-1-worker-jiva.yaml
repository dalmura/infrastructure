machine:
  install:
    extensions:
      - image: ghcr.io/siderolabs/iscsi-tools:v0.1.1

  kubelet:
    extraMounts:
      - destination: /var/openebs/local
        type: bind
        source: /var/mnt/attached_ssd
        options:
          - bind
          - rshared
          - rw
