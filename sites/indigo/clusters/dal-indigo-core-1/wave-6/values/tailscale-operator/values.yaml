# Set this if we ever move to headscale
# loginServer: ""

operatorConfig:
  image:
    repository: ghcr.io/tailscale/k8s-operator
  resources:
    requests:
      cpu: 10m
      memory: 96Mi

proxyConfig:
  image:
    repository: ghcr.io/tailscale/tailscale
