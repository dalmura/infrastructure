apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: peer-relay
spec:
  statefulSet:
    pod:
      tailscaleContainer:
        env:
          - name: TS_RELAY_SERVER_PORT
            value: "40000"
