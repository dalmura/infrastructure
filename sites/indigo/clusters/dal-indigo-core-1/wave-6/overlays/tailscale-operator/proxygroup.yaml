apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: ingress-proxies
spec:
  type: ingress
  proxyClass: peer-relay
  tags:
    - "tag:k8s"
    - "tag:k8s-indigo"
  replicas: 2
  hostnamePrefix: "dal-indigo-ts-relay"
