---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: reactive-resume-db
spec:
  description: "Reactive Resume DB"
  instances: 1
  enablePDB: false

  enableSuperuserAccess: true

  primaryUpdateStrategy: unsupervised

  imageCatalogRef:
    apiGroup: postgresql.cnpg.io
    kind: ClusterImageCatalog
    name: postgresql
    major: 18

  storage:
    storageClass: cluster-nobackup
    size: 10Gi

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: reactive-resume-postgres

  postgresql:
    parameters:
      archive_timeout: "30min"
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: reactive-resume-db-backup
spec:
  cluster:
    name: reactive-resume-db
  schedule: "0 0 0 * * *"
  backupOwnerReference: self
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
