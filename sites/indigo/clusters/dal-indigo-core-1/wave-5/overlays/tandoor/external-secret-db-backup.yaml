apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: "tandoor-db-backup"
spec:
  refreshInterval: "672h0m0s"  # 28d
  target:
    name: tandoor-db-backup
  dataFrom:
  - sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: VaultDynamicSecret
        name: "tandoor-db-backup"
